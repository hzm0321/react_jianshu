import reducer from "./reducer";
import * as actionCreators from './actionCreators';
import * as actionTypes from './actionTypes';

 // 出口文件,对store下各个文件暴露出口
export {reducer, actionCreators, actionTypes};